import React from 'react';

const NetworkPlayer = ({ currentPlayer, handleCellClick }) => {
  // Implement networking logic here to handle clicks from the network

  return null;
};

export default NetworkPlayer;
